# Google Tag Manager Container ID
# Get this from Google Tag Manager > Admin > Container Settings
# Format: GTM-XXXXXXXX
# GA4 is configured inside GTM rather than as a standalone gtag, so
# only this one variable is needed for analytics.
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXXX